services:
  web:
    build:
      context: .
      dockerfile: src/LightWiki.Wiki.Api/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: "DockerComposeDevelopment"
    ports:
      - "5000:80"
    volumes:
      - .:/code
      - logvolume01:/var/log
    links:
      - postgres
      - mongo
    depends_on:
      - mongo
      - postgres

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    restart: always
volumes:
  logvolume01: {}